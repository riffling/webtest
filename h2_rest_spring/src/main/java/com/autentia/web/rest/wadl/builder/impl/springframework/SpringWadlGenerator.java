package com.autentia.web.rest.wadl.builder.impl.springframework;

public class SpringWadlGenerator {

    // private final ApplicationBuilder applicationBuilder = null;
    // private final SchemaBuilder schemaBuilder = null;

    public SpringWadlGenerator() {
        // final ClassTypeDiscoverer classTypeDiscoverer = new
        // ClassTypeDiscoverer(new QNameBuilderFactory().getBuilder());
        // final GrammarsDiscoverer grammarsDiscoverer = new
        // GrammarsDiscoverer(classTypeDiscoverer);
        // final ApplicationContext appCtx = new ApplicationContext(new
        // SpringMethodContextIterator(handlerMapping),
        // grammarsDiscoverer);
        // applicationBuilder = new ApplicationBuilder(appCtx);
        // schemaBuilder = new SchemaBuilder(classTypeDiscoverer);
    }

    public static void main(String[] args) {

    }
}
